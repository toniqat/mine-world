# 오픈월드 인크리멘탈 지뢰찾기 — 설계 스펙

> **이 문서의 용도**: LLM 코딩 에이전트가 이 문서만 읽고 프로젝트를 구성할 수 있도록 작성된 설계 명세.
> **읽는 순서**: §0 요약 → §1 불변식 → §2~§6 코어 로직 → §7~§11 게임 시스템 → §12~§14 구현 계획.
> **원칙**: §1의 불변식은 어떤 경우에도 깨지 않는다. §15에 없는 파라미터 값을 임의로 정하지 말고, 미정이면 설정 파일에 노출하고 TODO로 표시한다.
> **구현 현황**: 2026-09-21 기준 Phase 0~4 구현 완료. 구현 중 확정·변경된 사항은 각 절의 `▶ 구현` 블록과 §17에 기록한다. 코드 기준 문서는 `CLAUDE.md`와 각 패키지 `README.md`.

---

## 0. 요약

### 0.1 한 줄 정의

경계가 없는 무한 지뢰찾기 월드에서, **지뢰는 위험물이 아니라 광맥**이다. 지뢰 위치를 논리로 확정하고 깃발을 꽂아 "소유"하면 시간당 수입이 발생하고, 그 수입으로 추론 자동화를 해금해 더 고밀도(=고수익·고난도) 지역으로 진출하는 인크리멘탈 게임.

### 0.2 핵심 루프

```
탐색(클릭) → 추론으로 지뢰 확정 → 깃발 → 영역 폐쇄 시 정산 → 수입
   ↑                                                          ↓
   └── 더 높은 밀도 지역 진출 ← 자동화/장비 해금 ←────────────┘
```

### 0.3 장르 전환 곡선 (의도된 것)

| 단계 | 플레이어가 실제로 하는 일 |
|---|---|
| 초반 | 순수 지뢰찾기. 한 칸씩 직접 추론 |
| 중반 | 자동화가 쉬운 패턴을 처리. 플레이어는 막힌 컴포넌트만 담당 |
| 후반 | 여러 드론의 정지 지점을 보고 **어디에 개입할지 고르는 트리아지 게임** |

후반의 게임은 "지뢰찾기"가 아니라 "지뢰찾기 회사 운영"이다. 이 전환은 버그가 아니라 목표다.

### 0.4 왜 이 조합이 성립하는가 (설계 근거)

1. **자동화 트리 = 솔버 계층.** 인크리멘탈이 자동화할 "지루한 반복"이 원본 게임에 이미 존재한다(단순 패턴 처리). 이걸 팔면 재미있는 부분(모호한 컴포넌트 판단)은 손상되지 않는다.
2. **지수 성장 축 = 지뢰 밀도.** 밀도가 오르면 수입은 지수적으로, 모호 컴포넌트 발생률은 초선형으로 증가한다. 자동화를 살수록 더 위험한 땅에 갈 수 있고, 거기선 자동화가 다시 부족하다 → 무한 루프.
3. **무한 보드는 강제 추측을 없앤다.** 유한 보드의 50/50은 강제되지만(다 열어야 이김), 무한 보드에선 우회할 수 있다. "찍기"가 도박에서 **우회 비용 판단**으로 바뀐다.

> ▶ **뒤집힘 (2026-09-21, 사용자 결정)**: 지형 벽(산·강, §3.3)이 이 명제를 의도적으로 깬다. 벽 칸은 타일도 지뢰도 숫자도 없어 유한 보드의 가장자리처럼 작동하므로, 벽에 붙은 모호 컴포넌트는 반대편에서 접근할 수 없고 **찍기가 강제**된다. 그 리스크를 크레딧으로 사는 정보 아이템(스캐너, §11)이 짝이 된다(스캐너 판매는 추후). 강제 추측을 하게 만드는 동기는 거점 정착(주변 8칸에 Unknown 없음, §9.3 구현 블록)이다.

---

## 1. 불변식 (INVARIANTS)

**이 절의 규칙을 깨는 구현은 거부한다.**

### INV-1. 깃발은 진실이 아니다
깃발(flag)은 플레이어의 **주장(claim)**일 뿐이며, 엔진의 제약 집합(constraint set)에 절대 포함되지 않는다.

- **엔진의 진실**: 공개된 숫자 칸 + 커밋된 오버라이드. 이것만.
- **믿음 상태(belief)**: 진실 + 깃발. 플레이어 UI와 드론이 사용.

두 솔버는 **같은 코드에 다른 입력**을 넣어 돌린다. 이 분리가 §10 오염 시스템 전체의 토대다.

### INV-2. 깃발은 즉시 피드백을 주지 않는다
깃발을 꽂거나 뺄 때 정답 여부를 알 수 있는 정보를 **어떤 경로로도** 노출하지 않는다. 수입 증가, 사운드, 이펙트, 통계 카운터, 애니메이션 전부 포함.

> 위반 시 결과: 플레이어가 깃발을 판정기로 써서 추론을 우회한다. 게임이 즉시 붕괴한다.

정산은 §9의 컴포넌트 폐쇄 시점에만 일괄 발생한다.

### INV-3. 드론은 플레이어보다 항상 명확히 약하다
자동 솔버는 구매한 티어까지만 적용하고, 모호해지면 **추측하지 않고 정지**한다. 드론에 확률 기반 추측을 주지 않는다.

- 드론 = 처리량(throughput)
- 플레이어 = 판단(judgment)

역할을 겹치게 하면 플레이어가 손을 놓아도 되고, 게임을 할 이유가 사라진다.

### INV-4. 지뢰는 관측되기 전까지 존재하지 않는다
칸의 진실값은 필요한 시점에 결정되며(§5), 결정된 값만 오버라이드 레이어에 커밋된다. 커밋된 값은 **절대 번복하지 않는다.**

### INV-5. 결정 불가 ≠ 50/50
가능한 해를 모두 세면 확률이 1/3, 2/3처럼 비대칭인 경우가 훨씬 흔하다. 확률 계산은 반드시 해 개수 기반으로 하며(§4.4), 미확정을 전부 50%로 표시하지 않는다.

### INV-6. 코어는 렌더러/프레임워크에 의존하지 않는다
`core/` 아래는 순수 로직이며 DOM, 캔버스, 엔진 API를 import하지 않는다. 헤드리스 시뮬레이션(§13 Phase 0)이 반드시 가능해야 한다.

---

## 2. 용어

| 용어 | 정의 |
|---|---|
| **cell** | 정수 좌표 `(x, y)`. 월드는 무한 2D 정수 격자 |
| **revealed** | 공개된 칸. 숫자값(0~8)을 가짐 |
| **unknown** | 미공개 칸 |
| **flag** | 플레이어/드론이 지뢰라고 주장한 unknown 칸 |
| **constraint** | `(cells: Set<cell>, n: int)` — cells 중 정확히 n개가 지뢰 |
| **frontier** | unknown이면서 revealed 숫자 칸과 인접한 칸의 집합 |
| **component** | frontier를 제약 공유 관계로 분할한 연결 성분. 독립적으로 풀 수 있는 단위 |
| **sealed** | 8방 이웃이 전부 revealed 또는 flag인 숫자 칸. CSP에서 제거 가능 |
| **closed** | 컴포넌트의 모든 unknown 칸이 revealed 또는 flag가 되어 컴포넌트가 소멸한 상태. 정산 트리거 |
| **ambiguous** | 현재 제약계의 해가 둘 이상이어서 값이 확정되지 않는 칸 |
| **base layer** | 해시 기반 결정론적 지뢰 배치 (§3.1) |
| **override layer** | 지연 결정/정산으로 확정된 칸의 희소 맵 (§3.2) |

---

## 3. 월드 생성

### 3.1 베이스 레이어 (무상태·결정론)

```
密度 d(x, y) = clamp(biomeNoise(seed, x, y), DENSITY_MIN, DENSITY_MAX)
isMineBase(x, y) = hash32(seed, x, y) / 2^32 < d(x, y)
```

- `hash32`는 좌표 해시(예: xxhash32 또는 splitmix 기반). 청크 경계 아티팩트가 없어야 하고, 인접 좌표 간 상관이 보이면 안 된다.
- 무상태이므로 무한 확장에 저장 비용이 0이다.

### 3.2 오버라이드 레이어 (희소·권위)

```ts
type Override = Map<CellKey, 0 | 1>;  // 0 = 안전, 1 = 지뢰
isMine(x, y) = override.get(key) ?? isMineBase(x, y)
```

지연 결정(§5)과 정산(§9)이 개입한 칸만 기록한다. 개입률이 낮으므로 탐색 면적 대비 저장량은 미미하다.

> **계측 필수**: 개입률(=오버라이드 항목 수 / 공개 칸 수)을 밀도별로 측정한다. 이 값이 5%를 넘으면 개입 정책이 너무 관대한 것이다.

### 3.3 밀도 바이옴

균일 랜덤 무한 평면은 금방 지루해진다. 밀도를 노이즈로 변조한다.

- **저밀도 평원**: 자동화로 거의 무인 운영. 이동 경로
- **고밀도 지대**: 수입 높음. 자동화가 계속 정지. 플레이어가 직접 붙어야 함
- **선형 구조**: 확정 지뢰로 이루어진 강/협곡. 통과 불가 또는 고비용. 탐색 경로 자체를 의사결정으로 만듦

> ▶ **구현 (지형 벽으로 교체)**: 강은 지뢰 띠가 아니라 **벽**이다. 벽 칸(`CellState.Mountain` / `Water`)은 열 수도 깃발을 꽂을 수도 없고, 지뢰가 아니며 숫자도 없다. CSP는 벽을 모든 모드에서 "확정 안전"으로 분류해 이웃 숫자의 제약에서 뺀다(유한 보드 가장자리와 동일). 생성은 시드 + 시작 칸의 무상태 함수(`World.terrain`, 청크 캐시)이며, 시작 칸이 정해지기 전에는 없고 시작 칸에서 `terrainMinRadius`(8) 안에도 없다. 산 = fBm(scale 16) > 0.68인 덩어리, 강 = |fBm(scale 96) − 0.5| < 0.009인 2~3칸 폭 띠이며, 여울(fBm(scale 10) < 0.32)에서 끊겨 강이 영역을 가두지 않는다. 면적 약 13 %, 40개 시드 중 시작 칸이 갇힌 경우 0. 거점망 연결선은 벽을 지나지 못하고, 거점 정착 판정은 벽을 해결된 칸으로 본다. 지형 도입 이전 세이브는 벽 없이(옛 지뢰 띠 강 유지) 불러온다.

---

## 4. CSP 모델과 솔버 계층

### 4.1 제약 추출

공개된 숫자 칸 `c`마다:
```
constraint = (c의 8방 이웃 중 unknown인 칸들, c.number - (c 주변 확정 지뢰 수))
```

**주의 (INV-1)**: "확정 지뢰"에 무엇을 넣는지가 엔진 솔버와 믿음 솔버를 가른다.
- 엔진 솔버: 오버라이드로 커밋된 지뢰만
- 믿음 솔버: 커밋된 지뢰 + 깃발

전역 지뢰 수 제약은 **존재하지 않는다**(무한 보드). 유한 보드 종반의 카운팅 추론은 §11의 스캐너 아이템으로 대체된다.

### 4.2 컴포넌트 분할

제약들을 "칸을 공유하는가"로 묶어 연결 성분을 만든다. 컴포넌트는 서로 독립이므로 각각 따로 푼다.

경험적으로 컴포넌트 하나는 수십 칸 이하다. 이것이 무한 보드에서도 매 클릭 계산량이 상수에 가까운 이유다.

### 4.3 솔버 티어

**이 티어는 그대로 업그레이드 트리로 판매된다 (§8).** 각 티어는 독립 함수로 구현하고, 활성 티어 집합을 인자로 받아 적용 범위를 제어한다.

| 티어 | 규칙 | 의사코드 |
|---|---|---|
| **T1** 단일 제약 | `n=0` → 전부 안전, `n=\|S\|` → 전부 지뢰 | 자명 |
| **T2** 부분집합 | `S₁ ⊂ S₂` → 새 제약 `(S₂\S₁, n₂-n₁)` 유도 후 T1 재적용 | 아래 |
| **T3** 소형 완전열거 | 컴포넌트 크기 ≤ `T3_MAX_CELLS`면 전수 탐색 | 아래 |
| **T4** 대형 열거 + 확률 | 크기 ≤ `T4_MAX_CELLS`. 확정 + 확률 산출 | §4.4 |

T2가 1-2-1, 1-2-2-1 등 사람이 손으로 하던 패턴 대부분을 커버한다.

> ▶ **구현** (`packages/core/src/solver.ts`): Phase 0 계측에서 T1 단독이 전체 검증의 70~93%를 차지해(§8.1의 분할 조건 충족) T1/T2를 각각 둘로 쪼개 **6티어**로 판매한다.
>
> | 티어 | 규칙 | 계측 근거 (30% 밀도, 로컬 봇 1000행동당 정지/피격) |
> |---|---|---|
> | **T1a** open | `n=0` → 전부 안전 | 단독: 594 정지 / 300 피격 |
> | **T1b** flag | `n=\|S\|` → 전부 지뢰 | +T1b: 99 / 41 |
> | **T2a** subset | `S₁ ⊂ S₂` → `(S₂\S₁, n₂−n₁)` 유도, 고정점까지 연쇄 | +T2a: 47 / 16 |
> | **T2b** pairwise | 교차 집합 I의 지뢰 수 구간 `[lo, hi]`로 직접 판정. 유도 제약은 만들지 않음 | +T2b: 29 / 7.7 |
> | **T3** enum | 컴포넌트 ≤ `T3_MAX_CELLS`(32) 완전 열거 | +T3: 30 / 6.7 (효과 미미 → T4 디딤돌 가격) |
> | **T4** prob | ≤ `T4_MAX_CELLS`(48) 열거 + 밀도 가중 확률 | +T4: 26 / 2.0 (확률이 찍기 품질을 올림) |
>
> 모든 검증에 생성 티어가 기록되어(`SolveResult.tier`) 계측과 UI가 같은 코드를 쓴다.

```
function tier2(constraints):
  repeat until no change:
    for each pair (c1, c2) with c1.cells ⊂ c2.cells:
      derive (c2.cells \ c1.cells, c2.n - c1.n)
      add if new
    apply tier1 to all constraints
```

```
function tier3or4(component, maxCells):
  if |component.cells| > maxCells: return UNDETERMINED
  solutions = []
  backtrack over component.cells assigning 0/1:
    prune: 어떤 제약도 이미 초과되거나 남은 칸으로 채울 수 없으면 가지치기
    on complete: if 모든 제약 만족: solutions.push(assignment)
  for each cell:
    if 모든 해에서 0 → SAFE
    if 모든 해에서 1 → MINE
    else → UNDETERMINED
```

**T3/T4 결과가 UNDETERMINED인 칸은 원리적으로 결정 불가능한 칸이다.** 더 나은 알고리즘으로 풀 수 있는 게 아니라 정보가 부족한 것이다. (해당 칸의 값이 서로 다른 유효 해가 둘 이상 존재 = 어떤 알고리즘도 판정 불가)

### 4.4 확률 (T4)

각 해를 **동일 가중으로 세면 안 된다.** 해마다 프론티어 밖 미탐색 영역에 남는 지뢰 배치 경우의 수가 다르다.

무한 보드에는 "남은 지뢰 총량"이 없으므로 유한 보드의 이항계수 가중은 그대로 쓸 수 없다. 대신:

```
weight(solution) = Π over cells: (해당 칸이 지뢰면 d(cell), 아니면 1 - d(cell))
P(cell = mine) = Σ weight(s where s[cell]=1) / Σ weight(s)
```

즉 베이스 레이어 밀도를 사전확률로 쓰는 베이지안 가중이다. 이는 §3.1의 생성 모델과 정확히 일치하므로 편향이 없다.

### 4.5 복잡도 (참고)

- 지뢰찾기 일관성 문제: NP-완전 (Kaye, 2000)
- "이 칸이 안전한가": coNP-완전
- 해 개수 세기(= 정확한 확률): #P-완전

최악의 경우는 감당 불가이나, 실제 컴포넌트가 작아 문제되지 않는다. `T4_MAX_CELLS` 초과 시 UNDETERMINED로 폴백하고 절대 블로킹하지 않는다.

---

## 5. 지연 결정 (Lazy Resolution)

### 5.1 언제 호출되는가

칸 `(x,y)`를 공개할 때:

```
function resolveCell(x, y):
  if override.has(key): return override.get(key)        // INV-4

  verdict = engineSolver.query(x, y)                     // 엔진 제약만 (INV-1)
  if verdict == SAFE:  commit(key, 0); return 0
  if verdict == MINE:  commit(key, 1); return 1

  // 여기서부터 ambiguous — 지연 결정 개입 구간
  base = isMineBase(x, y)
  if base == 0: commit(key, 0); return 0

  if interventionPolicy.shouldIntervene(x, y, component):
    commit(key, 0)
    densityDebt += 1                                     // §5.3
    return 0
  else:
    commit(key, 1)
    return 1
```

> ▶ **구현** (`packages/core/src/resolve.ts`, `world.ts`): 위 의사코드의 전제인 "숫자와 모순되지 않는 진실"을 다음 방식으로 보장한다.
> - `truth(x,y) = override ?? (hash < d) ?? 청크별 동결 부채 압력`은 항상 **모든 공개 숫자와 일치하는 완전 배정**이다. 베이스는 무상태, 오버라이드는 불변(INV-4), 부채 압력은 청크 최초 관측 시 동결되므로 값이 흔들리지 않는다.
> - 따라서 **안전한 공개 칸은 커밋하지 않는다** (오버라이드가 희소하게 유지됨, §3.2 계측 결과 ≤ 2%). 커밋은 개입·피격·스캐너·프로브에서만 발생한다.
> - 개입 시에는 해당 칸만 0으로 바꾸는 것이 아니라 **컴포넌트 전체를 `c=0`인 대안 해로 커밋**한다(§5.3의 "선택적 최적화"가 정합성 유지를 위해 필수가 됨). 현재 진실과의 해밍 거리가 최소인 해를 고르고, 깃발 칸은 가중치 3으로 변경을 기피한다.
> - 컴포넌트가 없거나(맹목 추측) `T4_MAX_CELLS`를 넘거나 열거가 실패하면 개입하지 않는다.

### 5.2 개입 정책

무한 보드에서는 **무추측 보장이 불필요하고, "강제 추측 없음"만 보장하면 된다.** 애매한 컴포넌트는 우회해서 반대편에서 접근하면 제약이 추가되어 해소되기 때문이다.

판정 기준 = **우회 가능성**:

```
function shouldIntervene(x, y, component):
  // 이 모호 영역이 구조적으로 영원히 해소 불가능한가?
  region = floodFill over unknown cells from (x,y), 상한 ESCAPE_CAP 칸
  if flood fill이 ESCAPE_CAP에 도달했거나 미탐색 개방 공간에 닿음:
      return false   // 우회로 해소 가능 → 플레이어가 우회했어야 함
  else:
      return true    // 공개 칸/확정 지뢰로 완전히 둘러싸인 고립 영역 → 구제
```

정책은 **교체 가능한 전략 객체**로 구현한다:

| 모드 | 동작 | 용도 |
|---|---|---|
| `STRICT` | 절대 개입 안 함 | 하드코어 / 계측 기준선 |
| `FAIR` | 위 우회 가능성 판정 | **기본값** |
| `FORGIVING` | 첫 모호 피격 N회 구제 | 초반 온보딩 |

> ▶ **구현 (지형)**: FAIR는 flood fill이 벽에 닿으면(대상 칸 이웃 포함) 개입하지 않는다. 벽에 막힌 50/50은 지형이 의도한 강제 추측이므로 구제하지 않고, 공개 칸·확정 지뢰로만 둘러싸인 기존 고립 포켓만 구제한다.

### 5.3 밀도 부채

개입할 때마다 지뢰를 하나 없애면 플레이어 경로 주변 밀도가 계속 내려가 월드가 밋밋해진다.

```
densityDebt: int  // 전역 또는 지역 단위

// 미탐색 영역 밀도 변조
d_effective(x, y) = d(x, y) * (1 + debtPressure(x, y))
```

- 상환은 **미탐색 영역의 밀도를 일시적으로 올려서** 한다. 이미 공개된 숫자와 충돌할 수 없으므로 항상 안전하다.
- (선택적 최적화) 같은 컴포넌트 내의 다른 미확정 칸을 지뢰로 올려 즉시 상쇄할 수 있으나, 그 칸이 최소 하나의 유효 해에서 지뢰여야 한다. 검증 비용이 있으니 Phase 3 이후에 고려.

> ▶ **구현**: 부채는 `densityDebt`(전역)에 누적되고, **청크(16×16)를 처음 관측하는 순간** 그 청크의 압력 `p = min(DEBT_PRESSURE_MAX, debt / (256·d))`를 동결하며 기대 지뢰 수만큼 부채를 차감한다. 이미 관측된 청크는 영향받지 않으므로 공개 숫자와 충돌할 수 없고, 커밋 없이도 진실이 안정적이다. 컴포넌트 대안 해 커밋이 상쇄를 일부 수행하므로(위 §5.1 구현 블록) 실제 부채는 작다.

---

## 6. 프론티어 관리와 GC

제약은 미공개 이웃이 남아 있는 동안만 유효하다.

```
function trySeal(numberedCell):
  if 8방 이웃이 전부 (revealed || flagged):
    제약 집합에서 제거
    (숫자 표시값은 유지, CSP에서만 제거)
```

**결과**: 활성 제약 집합의 크기는 탐색 **면적**이 아니라 탐색 영역의 **둘레**에 비례한다. 무한 월드에서도 메모리·계산량이 통제된다.

봉인된 내부 영역은 디스크/IndexedDB로 내리거나, 렌더링에 필요한 숫자만 남기고 CSP에서 완전히 잊어도 된다.

---

## 7. 지형 상호작용

### 7.1 지뢰를 밟았을 때

**즉사 금지.** 무한 보드에 어울리지 않는다.

- 체력 또는 자원 손실
- 결과: "기댓값상 20% 지뢰는 밟고 지나가는 게 이득"이라는 계산이 성립
- 확률 추론이 **처벌이 아니라 전략**이 된다 (INV-5와 §11 확률 표시가 여기서 의미를 가짐)

밟은 지뢰는 소실되며 수입원이 되지 않는다(깃발로 확정한 것만 자산).

> ▶ **구현** (사용자 결정, Sapper 방식): 체력 없음. 정산 보상은 **미정산(unbanked) 자금**에 쌓이고 클릭 연속(streak) 배율이 붙는다. 지뢰를 밟으면 미정산 자금의 `MINE_HIT_LOSS_FRACTION`(0.5)을 잃고 연속이 끊긴다. **Cash Out**으로 크레딧에 은행 처리한다. 소유 지뢰의 초당 수입은 미정산을 거치지 않고 크레딧으로 직행한다.

---

## 8. 업그레이드 트리

### 8.1 추론 자동화 (핵심 축)

§4.3 솔버 티어를 그대로 판매한다.

| 구매 항목 | 효과 | 체감 |
|---|---|---|
| T1 자동화 | `3` 주변 미공개 3칸 → 자동 깃발 등 | 지루한 반복 제거 |
| T2 자동화 | 1-2-1, 1-2-2-1 자동 처리 | 여기서 체감이 급상승 |
| T3 자동화 | 소형 컴포넌트 완전 열거 | 중형 덩어리 해소 |
| T4 자동화 | 대형 열거 + **확률 표시** | 판단 보조 (자동 추측 아님, INV-3) |

**티어 분할 주의**: T2 하나로 전체 칸의 90% 이상이 처리되면 업그레이드 배분이 앞쪽에 쏠린 것이다. §13 Phase 0에서 티어별 처리 비율을 측정한 뒤, 필요하면 T2를 패턴별로 쪼개 판다(1-1 규칙 / 1-2-1 / 일반 부분집합 …).

> ▶ **구현**: 쏠림은 T2가 아니라 **T1**에서 나타났다(전체 검증의 70~93%). 그래서 판매 항목은 §4.3 구현 블록의 6개(`auto_t1a` ~ `auto_t4`)이며, T1a 구매가 첫 드론을 해금한다. 가격은 §15 참고(플레이 후 조정 대상).

### 8.2 기타 축

- **드론 수 / 속도 / 작업 반경**: 처리량

> ▶ **구현**: 작업 범위는 정사각형이 아니라 타일 단위로 반올림한 원(`dx²+dy² ≤ r²+r`, `inDisc`)이다. 실행 직전에 판정을 다시 확인하고, 그사이 판정이 사라졌으면 행동 없이 대상을 다시 고른다.
>
> ▶ **구현 (2026-09-21 개정, 드론이 너무 많이 푼다는 플레이 피드백)**: 드론은 **소유 지뢰(Owned) 위에만** 선다. 해금/추가 시 주변 Unknown이 많은 소유 지뢰 상위 8개 중 하나에 자동 배치되고(시드 기반 무작위), 소유 지뢰가 없으면 대기(`idle`)한다. 행동은 물리적으로 보인다: 거점에서 대상 칸까지 킹 이동 경로(대각선 먼저, 꺾임 최대 1회)를 미리 계산하고 반투명 선이 타일마다 서서히 뻗어가며(`baseMoveTilesPerSec`), 선이 닿으면 게이지(`acc`)를 채운 뒤 실행한다. 거점 원 안에 확정 칸이 없으면, 확정 칸을 원 안에 들이는 다른 소유 지뢰로 선을 뻗고 그 선을 타고 이동한다. 이동은 처음 배치된 지뢰 기준 반경 안으로 제한되므로(목줄) 드론의 도달 범위는 최대 2r이다. 확정 칸도 이동할 지뢰도 없으면 빨간색으로 정지한다. 쓰이지 않은 선은 뻗을 때의 반대로 줄어들며 사라진다(클라이언트 연출). 플레이어는 드론을 드래그해 다른 빈 소유 지뢰에 놓을 수 있고, 드래그 중에는 드론이 멈춘다. 이전의 "플레이어 마지막 행동을 따라다니는 어시스턴트"는 제거했다.
- **이동/시야**: 탐색 범위
- **스캐너 (§11)**: 정보 아이템
- **감사(Audit) (§10)**: 오염 탐지

---

## 9. 경제: 컴포넌트 단위 정산

### 9.1 왜 즉시 정산이 아닌가

INV-2 참조. 즉시 정산은 깃발을 판정기로 만든다.

### 9.2 정산 트리거

컴포넌트가 **closed** 될 때 = 컴포넌트의 모든 unknown 칸이 revealed 또는 flagged가 되었을 때.

이 시점에는 모든 깃발 칸의 진실값이 **제약으로부터 산술적으로 강제**된다(주변이 전부 공개되었으므로). 따라서 추가 정보 없이 정오 판정이 가능하다.

```
function settle(component):
  truth = engineSolver.forceAll(component)   // 이제 유일해가 강제됨
  for each flag f in component:
    commit(f.key, truth[f])
    if truth[f] == 1:
      f → 생산 시설로 활성화, 수입 풀에 편입
      payout += mineValue(f)                 // §9.3
    else:
      f → 소멸 (보상 없음)
  emit SettlementEvent(component, 정답 수, 오답 수)
```

> ▶ **구현 변경** (`packages/core/src/game.ts`): 정산 단위를 CSP 컴포넌트가 아니라 **claim batch**로 바꿨다.
> - 이유: public 모드 컴포넌트는 깃발을 공유 칸으로 삼아 프론티어 전체로 이어지므로, 30% 밀도 계측에서 3000칸 동안 폐쇄가 0회였다.
> - 규칙: 깃발 f는 **f에 인접한 모든 공개 숫자가 봉인**(미공개 이웃 없음)되면 정산 가능하다. 정산 가능한 깃발들을 봉인 숫자 공유 관계로 묶은 것이 한 batch이며, 같은 액션에서 함께 판정된다. 판정은 `world.truth()`(정합성 불변식으로 강제됨).
> - **오답 깃발 페널티** (스펙 추가): batch에 오답이 하나라도 있으면 그 batch의 진짜 지뢰는 전부 **몰수(`Lost`)**, 보상 0, 오답 깃발 수만큼 미정산 자금을 `WRONG_FLAG_LOSS_FRACTION`(0.25)씩 소각한다. 이것이 없으면 "전부 깃발 꽂기"가 무료 프로브가 된다.
> - INV-2는 유지된다: 피드백은 여전히 정산 시점에만 발생한다.
> - **정산은 여는 행동에서만** (2026-09-21): 깃발을 꽂거나 뽑는 행동으로는 아무것도 정산되지 않는다(깃발 하나로 기존 깃발이 곧바로 거점이 되지 않음). 깃발이 바뀐 칸은 기억해 두었다가(`Game.deferred`, 저장됨) 어디서든 다음 여는 행동 때 주변에서 정산 가능해진 깃발을 정산한다.
> - **코드(chord)는 구제하지 않는다** (2026-09-21): 숫자를 눌러 주변을 열 때는 FAIR/FORGIVING 개입이 없다. 깃발을 믿고 여는 것이므로 잘못 꽂은 깃발이 있으면 진짜 지뢰가 터진다.

### 9.3 수입

```
mineValue(cell) = BASE_VALUE * densityMultiplier(d(cell))
```

`densityMultiplier`는 밀도에 대해 **지수적**으로 증가한다. 이것이 §0.4의 성장 축이다.

> **▶ 구현 (거점, 2026-09-21)**: 소유 지뢰 = 거점, 첫 시작 칸 = 주 거점. 패시브 수입은 거점별 생산량의 합이다: `rate = mineValue × incomePerSecondMult × levelMult(L) × (1 + linkBonus(L) × 직접 연결 수)`. 연결은 체비셰프 거리 ≤ 두 거점 연결 범위 중 큰 값. 업그레이드는 주 거점만(레벨 L = 네트워크 레벨: 생산 배율·연결 범위·연결당 보너스). 주 거점은 densityMin 거점의 2배를 첫 클릭부터 생산하고 비용도 2배 기준, 연결 범위 2배. 파라미터 `config.bases`, 코드 `packages/core/src/econ/bases.ts`.
>
> **▶ 구현 (거점 네트워크 개정, 2026-09-21)**: 연결 범위와 연결 보너스를 제거했다. `rate = mineValue × incomePerSecondMult × levelMult(L)`. 네트워크는 주 거점을 루트로 하는 트리: 각 거점의 다음 거점(부모)은 자기보다 주 거점에 (맨해튼 거리로) 더 가까운 거점 중 가장 가까운 것(동률은 주 거점에 더 가까운 쪽, 다음은 키). 획득 순서와 무관하므로 증분 추가와 전체 재계산이 같다. 연결선은 자식에서 가로 먼저, 세로로 한 번 꺾이는 ㄱ자 경로(타일 단위, 두께 1/3 타일, 투명도 약 90%). 거점 생산물은 거점에 쌓였다가 `shipInterval`마다 출발해 `transportTilesPerSec`로 간선을 따라 이동하고, 중간 거점에서 바로 다음 간선으로 이어간다(막 출발한 화물이 있으면 합류). **크레딧은 화물이 주 거점에 닿을 때만 오른다** — 멀수록 늦게 도착. 주 거점 자체 생산은 즉시 입금. 저장 시 미도착분은 로드 때 즉시 입금. 업그레이드는 생산 배율만 올린다.

> **▶ 구현 (단지·고립 거점, 2026-09-21)**: 서로 1칸 이내(대각선 포함, 연쇄)의 거점은 하나의 **단지**로 묶여 네트워크에서 한 노드처럼 동작한다. 단지 내부 이동은 즉시이며, 주 거점이 속한 단지(주 단지)의 생산은 즉시 입금. 단지의 허브는 주 거점에 가장 가까운 구성원이고, 부모 단지는 허브가 더 가까운 단지 중 구성원 간 맨해튼 거리가 가장 짧은 단지(동률: 부모 허브가 주 거점에 더 가까운 쪽 → 도착 키 → 출발 키). 간선은 출발 구성원(exit)에서 도착 구성원(entry)까지의 ㄱ자 경로. 단지의 어느 구성원도 상하좌우에 열린 칸(숫자/빈칸)이 없으면 **고립**: 생산 0, 연결 없음, 반투명 표시. 옆 칸이 열리면 즉시 해제(드론 배치는 고립과 무관). 거점이 바뀌거나 고립 거점 옆 칸이 열리면 전체 재계산(증분 추가 제거). 클라이언트: 2칸 이상 단지는 구성원 타일을 매우 희미한 강조색으로 칠하고, 대각선으로만 닿은 곳은 작은 삼각 브리지로 이어 하나의 큰 거점처럼 보이게 한다. 화물 연출은 최대 불투명도 45 %, 앞쪽이 부드럽게 올라오고 꼬리가 부드럽게 사라지는 그라데이션.

> **▶ 구현 (폭발·복구·인크리멘탈 기본 구성, 2026-09-21)**: (1) **단지 = 상하좌우 연쇄만**. 대각선으로만 닿은 거점은 별도 단지(주 거점 단지 포함). 2칸 이상 단지는 희미한 강조색 칠 + 단지 전체를 두르는 테두리로 표시(삼각 브리지 제거). (2) **주 거점은 직접 생산하지 않는다**. 주 거점 레벨업마다 활성 거점 전체 생산 ×1.15(`levelProdGrowth`). (3) **지뢰 폭발**: 지뢰를 밟으면(미정산 자금 손실은 유지) 그 칸을 중심으로 원형 폭발. 반경은 주 거점으로부터의 거리 5칸 구간마다 최소·최대가 번갈아 1씩 오르는 범위(3–5, 4–5, 4–6, 5–6, …) 안에서 균등 무작위(`econ/blast.ts`, `config.blast`). 범위 안 거점은 **비활성**: 생산 0, 단지·네트워크에서 제외(거점으로 판정하지 않음), 그 단지의 적재분은 소실. 주 거점은 면역. (4) **복구**: 비활성 거점을 클릭해 `repairCostBase + repairCostPerTile × 열린 칸 수` 크레딧으로 복구. (5) **업그레이드 축소**: 주 거점 레벨(주 거점 패널 헤더), 운송 속도(`transport_speed`), 연속 보너스 상한(`streak_cap`)만 판매. T1a~T4 자동화·드론·소모품(감사·스캐너·프로브)과 확률 오버레이는 제거. 드론 로직은 장착 아이템용으로 core에 유지(`Game.equipDrones`). (6) UI: 우측 상단 업그레이드 버튼은 주 거점과 같은 다이아몬드 아이콘이며, 주 거점 클릭과 같은 패널을 연다(제목 = 주 거점 + 레벨, 헤더 우측 = 레벨업 비용·버튼, 본문 = 네트워크 생산·활성/고립/비활성 거점 수·다음 레벨 배율·업그레이드). 도착한 화물의 꼬리는 함께 사라지지 않고 잔상으로 남아 서서히 사라진다.
>
> **▶ 구현 (턴 생산, 2026-09-21)**: 거점 생산은 시간이 아니라 **턴** 단위다. 플레이어가 타일을 바꾸는 행동 1회(클릭 한 번, 연쇄 포함; 무언가를 여는 코드 한 번; 지뢰를 밟아도; 깃발 꽂기) = 1턴이며, 턴마다 연결된 모든 거점의 생산량이 즉시 크레딧으로 들어온다(`Bases.turn`). 깃발을 걷거나(깃발 → "?"), "?"를 지우거나, 아무것도 열지 못한 숫자 클릭, 거점 선택은 턴이 아니다(2026-09-22 변경: 이전에는 깃발도 턴이 아니었다). 거점당 턴 생산 = `mineValue × incomePerTurnMult(0.02, 이전 초당 값) × levelMult × 대단지 배율`. 연결선 위 화물은 연결되어 있음을 보여주는 연출일 뿐이며(턴마다 생산 중인 단지가 하나씩 출발, 크레딧을 싣지 않음) 운송 속도 업그레이드(`transport_speed`)와 `shipInterval`은 제거했다.

> **▶ 구현 (단지 재정의·경로·대단지, 2026-09-21)**: (1) **정착 거점**: 주변 8칸에 Unknown·깃발이 없고, 그 안의 숫자가 모두 완전히 사라진 상태(숫자 옆에도 Unknown·깃발 없음)인 거점. 정착 거점끼리 체비쇼프 거리 2칸 이내(상하좌우·대각선, 한 칸 틈 포함, 연쇄)면 하나의 단지. 정착하지 않은 거점은 혼자 단지(생산은 계속). (2) **연결 경로는 Unknown·깃발 칸을 지나지 않는다**(4방향 타일 경로, 열린 칸·거점·잃은/폭발 지뢰는 통과). 통과 가능한 칸은 다시 막히지 않으므로 경로가 끊기지 않는다. 부모 = 허브가 주 거점에 (맨해튼) 더 가까운 단지 중 이 경로로 가장 가까운 단지(BFS, 동률: 부모 허브가 더 가까운 쪽 → 도착 키), 경로는 가능한 한 직진. (3) **고립 = 그런 경로로 주 단지까지 이어지지 않는 단지**(상하좌우 열린 칸 규칙 대체). 칸이 열릴 때(열림·소유·잃음·폭발)마다 재계산. 깃발은 네트워크에서 Unknown과 똑같이 취급되므로 깃발을 꽂거나 떼는 것은 재계산하지 않는다. (4) **대단지**: 거점 5개 이상(`grandMinBases`) 단지는 구성원 생산 ×(1 + 0.05 × 거점 수)(`grandBonusPerBase`). 새로 형성되면(이전에 대단지였던 구성원이 없을 때, 로드 직후 제외) `grand` 이벤트 → 단지 범위 중앙에서 퍼지는 링 두 개 + 떠오르는 라벨, 구성원 펄스, 토스트. (5) 단지 테두리 불투명도 0.6 → 0.3, 단지 칠은 구성원 + 도달 범위 내 구성원 쌍 사이 직사각형.

**감쇠(decay) 메커닉을 넣지 말 것.** 밀도가 지수 축이므로, 오래된 저밀도 영토는 자연히 반올림 오차가 된다. 인위적 감쇠는 "유지보수 잡무" 안티패턴이다.

### 9.4 부수 효과 (의도된 것)

"영역을 깔끔하게 닫는 것"이 보상 단위가 되면서, 지뢰찾기 숙련자가 원래 하던 플레이(한 구역을 완전히 정리하고 넘어가기)가 그대로 경제 행위가 된다.

---

## 10. 오염 (Contamination)

### 10.1 메커니즘

INV-1, INV-3의 직접적 귀결이다.

1. 플레이어가 틀린 깃발을 꽂는다 (즉시 피드백 없음, INV-2)
2. 드론은 **믿음 솔버**로 동작하므로 그 깃발을 참으로 놓고 주변을 전개한다
3. 드론이 "안전하다"고 판단한 칸을 공개한다
4. 엔진은 **엔진 솔버**(깃발 제외)로 resolve → 강제 안전이 아니면 베이스/지연 결정 → 지뢰일 수 있다
5. 폭발. 연쇄적으로 잘못된 전개가 드러난다

### 10.2 왜 좋은가

- **대가가 지연되고 증폭된다** — 인크리멘탈의 리듬과 맞는다
- **자동화 규모가 클수록 정확성의 가치가 오른다** — 성장이 신중함을 요구하는 구조
- **사고 조사가 재미있다** — 어디서부터 틀렸는지 역추적하는 것 자체가 추론 게임

### 10.3 완화 수단 (판매 아이템)

- **Audit**: 내 영토의 틀린 깃발 개수를 알려줌
- **Audit+**: 틀린 깃발이 있는 **구역**까지 좁혀줌 (정확한 칸은 알려주지 않음)
- **드론 안전 모드**: 드론이 T1/T2 결과 중 깃발에 의존하지 않는 추론만 사용 (느리지만 오염 면역)

---

## 11. 정보 아이템: 스캐너

무한 보드에는 전역 지뢰 수 제약이 없다(§4.1). 유한 보드 종반의 카운팅 추론이라는 스킬이 통째로 사라지므로, 소모품으로 되돌려준다.

- **Scanner(r)**: 주변 `(2r+1)×(2r+1)` 범위의 지뢰 총 개수를 알려줌
  - 구현: 해당 범위에 `(cells, n)` 제약을 **엔진 제약으로 추가**. 그러면 솔버가 자동으로 활용한다. 별도 로직 불필요
  - 주의: 이 제약은 진실이어야 하므로, 아직 미결정인 칸들에 대해 베이스 레이어 기준 개수를 세고 그 칸들을 **즉시 커밋**한다(INV-4 유지)
- **Probe**: 특정 한 칸의 진실을 공개 (비쌈)

효과: 모호한 컴포넌트를 만났을 때 **우회할 것인가 / 아이템을 쓸 것인가 / 확률에 걸 것인가**라는 3지 선택이 생긴다. 이것이 이 게임의 중심 긴장 구조다.

---

## 12. 프레스티지

무한 월드라 "리셋"이 자연스럽지 않다. 둘 중 하나를 고른다(Phase 4에서 결정).

### A. 영토 청산 (Liquidation)
확보한 모든 지뢰를 폭파해 영구 재화로 전환하고 새 시드로 시작.
```
prestigeGain = Σ over owned mines: densityMultiplier(d(cell))
```
깊이 들어간 플레이가 보상받는다. 표준적이고 계산이 명확하다.

### B. 심층 이주 (Descent)
리셋 없이 훨씬 고밀도인 새 레이어로 이동. 기존 영토는 낮은 배율로 계속 수입.
- 장점: 상실감 없음
- 단점: 상태가 무한 누적 → 저장/성능 문제

**권장: A로 시작.** B는 저장 전략이 검증된 후에.

> ▶ **구현**: A안 확정. `cores = floor(Σ densityMultiplier / PRESTIGE_DIVISOR)`, 코어당 지뢰 가치 +10%. 소유 지뢰 50개 이상일 때 통계 패널에서 청산 가능. 새 시드로 재시작하되 코어·누적 통계는 유지.

---

## 13. 구현 계획

### 13.1 모듈 구조

```
core/                    # 순수 로직, 렌더러 의존 금지 (INV-6)
  hash.ts                # hash32, biomeNoise, density(x,y)
  world.ts               # base layer + override layer, isMine(), commit()
  csp.ts                 # 제약 추출, 컴포넌트 분할, seal/GC
  solver.ts              # T1~T4. (constraints, tiers) → verdicts + probabilities
  resolve.ts             # 지연 결정, 개입 정책 전략, 밀도 부채
  game.ts                # reveal / flag / settle 트랜잭션
econ/
  income.ts              # mineValue, 수입 틱
  upgrades.ts            # 업그레이드 정의 + 가격 곡선
  contamination.ts       # 감사 아이템
agents/
  drone.ts               # 믿음 솔버 기반 자동화. 모호 시 정지 (INV-3)
sim/                     # ★ Phase 0의 산출물
  harness.ts             # 헤드리스 시뮬레이션 러너
  metrics.ts             # §13.3 지표 수집
ui/
  ...                    # 마지막에
```

### 13.2 권장 스택 (교체 가능)

- **언어**: TypeScript. 코어를 순수 함수로 유지하면 계측 하네스와 UI가 같은 코드를 쓴다
- **렌더링**: 무한 스크롤 격자 → Canvas 2D 또는 WebGL 인스턴싱. **DOM 노드로 칸을 그리지 말 것**
- **저장**: IndexedDB. 오버라이드 맵 + 봉인 영역 숫자
- **UI 상태**: 코어와 분리. 코어는 이벤트만 emit

코어가 §13.1대로 분리되어 있으면 스택 변경 비용은 낮다.

> ▶ **구현** (npm workspaces 모노레포, 사용자 결정):
> ```
> packages/core/src/          hash · world · board · csp · solver · resolve · game · events · save
>   econ/                     income · upgrades · contamination
>   agents/                   drone
> packages/core/test/         solver.test.ts · invariants.test.ts (vitest)
> packages/sim/src/           bot(Global/Local) · harness · metrics · run   → results/phase0.md|csv
> packages/web/src/           Vite + TypeScript + PixiJS(WebGL). render/(textures·camera·boardView) · ui/(hud·panels·toast·dom) · input · theme · i18n(ko/en) · storage(IndexedDB)
> packages/electron/src/      main.ts(CommonJS) · preload.cjs. 개발 실행만, 패키징 없음
> ```
> 렌더링은 셀당 스프라이트 1개를 16×16 청크로 묶어 컬링한다(DOM 노드 없음). UI 테마는 VS Code Light/Dark Modern 팔레트. 코드·주석·문서는 영어, UI 문자열은 ko/en.

### 13.3 개발 단계

#### Phase 0 — 헤드리스 코어 + 계측 (★ 최우선)

**게임을 만들기 전에 두 개의 숫자를 측정한다. 이 표가 나오면 밸런싱이 거의 기계적으로 따라온다.**

산출물:
1. `core/hash`, `core/world`, `core/csp`, `core/solver`(T1~T4), `core/resolve`
2. `sim/harness` — 자동 플레이 봇이 정해진 티어로 N칸 탐색

측정 지표:

| 지표 | 왜 필요한가 | 예상 |
|---|---|---|
| **밀도별 모호 컴포넌트 발생률** | 난이도 곡선 + 밀도별 수입 배율의 근거 | 20% 근처에서 급격히 꺾임. 그 지점이 게임의 중반 벽 |
| **티어별 자동 처리 비율** | 업그레이드 배분 | T2에서 90% 초과면 티어를 더 쪼개야 함 |
| 개입률 (오버라이드/공개 칸) | 개입 정책 검증 | 5% 초과면 정책이 관대함 |
| 컴포넌트 크기 분포 | T3/T4 상한 결정 | p99가 `T4_MAX_CELLS` 안에 들어와야 함 |
| 클릭당 솔버 시간 | 성능 | 밀도 35%에서도 상수 시간에 가까워야 함 |

**수용 기준**: 위 5개 지표를 밀도 8/12/16/20/25/30/35%에서 각각 출력하는 CSV/표가 나온다.

> ▶ **완료** (`npm run sim` → `packages/sim/results/phase0.md`). 로컬 봇(드론 반경 8) 기준 요약:
>
> | 밀도 | 정지/1000행동 | 강제 추측 | 모호 컴포넌트 | 컴포넌트 p95/p99 | 피격/1000칸 | 지뢰/1000칸 |
> |---|---|---|---|---|---|---|
> | 8% | 17 | 0% | 16% | 17/33 | 0 | 11 |
> | 12% | 13 | 0% | 21% | 30/40 | 0 | 45 |
> | 16% | 11 | 0% | 34% | 34/45 | 0 | 108 |
> | 20% | 9 | 0% | 51% | 34/50 | 0 | 153 |
> | 25% | 13 | 0% | 69% | 30/43 | 0 | 223 |
> | 30% | 35 | 1.9% | 82% | 28/38 | 2.8 | 285 |
> | 35% | 77 | 6.4% | 90% | 23/34 | 14.4 | 339 |
>
> - 중반 벽은 예상(20%)보다 늦은 **25~30%**에서 시작한다.
> - 오버라이드율 ≤ 2% (기준 5% 이내). FAIR 개입은 봇에서 0회(봇은 열린 프론티어에서만 찍으므로); 사람의 고립 포켓용.
> - 솔버 시간 p99 < 4 ms(35%에서 최대 21 ms).
> - 8%는 사실상 빈 땅(1000칸당 지뢰 11개)이라 월드 밀도 하한을 10%로 올렸다.
> - 무한 보드의 0 캐스케이드는 저밀도에서 끝나지 않으므로 반경 24 / 800칸 상한을 두었다(`play.cascadeRadius/Cap`).

#### Phase 1 — 플레이 가능한 무한 지뢰찾기

경제 없음. 지뢰찾기만.
- 무한 스크롤 렌더링, 클릭/깃발, 숫자 표시
- 체력 시스템 (§7.1)
- 자동화 없음 — **이 상태에서 재미있어야 한다.** 재미없으면 인크리멘탈을 얹어도 안 된다

**수용 기준**: 밀도 12% 월드에서 20분간 손으로 플레이했을 때 부당한 죽음이 없다.

> ▶ **구현 완료, 수동 플레이 검증은 미실시.** 숫자 정합성(모든 공개 숫자 = 주변 진실 지뢰 수)은 STRICT/FAIR/FORGIVING × 다수 시드에서 테스트로 고정했다. "재미있는가"는 사람이 확인해야 한다.

#### Phase 2 — 경제

- 컴포넌트 폐쇄 감지 + 정산 (§9)
- 수입 틱, 자원 표시
- T1/T2 자동화 구매

**수용 기준**: INV-2 검증 테스트 — 깃발을 꽂았다 뺐다 하며 관측 가능한 모든 상태를 diff해도 정답 정보가 새지 않는다.

> ▶ **완료.** `invariants.test.ts`의 INV-2 테스트가 저장 데이터 전체 스냅샷 + 이벤트 발생 여부를 diff한다. T1a 구매 시 드론(소유 지뢰 위에 자동 배치, §8.2 구현 블록)이 생겨 Phase 2의 "자동화 구매"와 Phase 3의 드론이 같은 객체로 이어진다.

#### Phase 3 — 자동화와 오염

- 드론 (믿음 솔버, 정지 동작)
- 오염 연쇄 (§10)
- 감사 아이템
- 밀도 바이옴 (§3.3), 스캐너 (§11)

**수용 기준**: 의도적으로 틀린 깃발 1개를 심었을 때, 드론이 그것을 기반으로 전개하다 폭발하고 플레이어가 로그로 원인을 역추적할 수 있다.

> ▶ **완료.** 같은 시나리오가 테스트로 고정되어 있다. 드론 폭발 로그(`drone_explode`)에는 판단 근거였던 인근 깃발 목록(basis)이 들어가고, UI 로그·드론 패널에서 좌표 칩을 눌러 이동할 수 있다. 폭발한 드론은 수동 재가동 전까지 중단된다. 드론 안전 모드는 public 모드(깃발 무시)로 추론한다.

#### Phase 4 — 트리아지와 프레스티지

- 막힌 드론 목록 UI (예상 수익 / 난이도 / 필요 아이템)
- T4 확률 표시
- 프레스티지 (§12 A안)

> ▶ **완료.** 드론 패널이 정지한 드론마다 막힌 컴포넌트를 기대 가치순으로 보여준다(크기·미확정 수·최소 확률·권장 수단: 찍기/스캐너/프로브/우회). 확률 오버레이는 T4 보유 시 뷰포트를 250 ms 간격으로 belief 모드로 풀어 표시한다. 청산은 통계 패널.

---

## 14. 테스트 항목

불변식은 테스트로 고정한다.

| ID | 테스트 |
|---|---|
| T-INV1 | 엔진 솔버 호출 시 제약 집합에 flag가 포함되지 않음 (단위 테스트로 주입 검증) |
| T-INV2 | 깃발 토글 전후 전체 관측 가능 상태 스냅샷 diff = 깃발 자체 외 변화 없음 |
| T-INV3 | 드론은 UNDETERMINED 칸을 절대 공개하지 않음 |
| T-INV4 | 커밋된 오버라이드는 재기록되지 않음 |
| T-INV5 | 비대칭 확률 케이스(1/3, 2/3)가 실제로 1/3, 2/3으로 산출됨 |
| T-SOLVE | 알려진 패턴 픽스처: 1-2-1은 풀림, 벽면 1-1은 안 풀림 (지형 벽이 생긴 뒤로 무한 평면에서도 벽면 1-1이 발생함 — 의도) |
| T-TERRAIN | 벽은 시작 전·시작 칸 근처에 없고 지뢰가 아님, 제약에서 제외, FAIR는 벽 접촉 포켓을 구제하지 않음, 벽과 부딪치는 플레이에서도 숫자 정합성 유지, 거점 연결선 차단·정착 판정, 저장 왕복·구 세이브 벽 없음 |
| T-SEAL | 봉인 후 활성 제약 수가 탐색 면적이 아닌 둘레에 비례 |
| T-SETTLE | 폐쇄된 컴포넌트는 항상 유일해로 강제됨 (랜덤 시드 1000회) |

---

## 15. 파라미터 (미정 — Phase 0에서 결정)

**아래 값을 임의로 확정하지 말 것.** 설정 파일에 노출하고 Phase 0 계측 결과로 채운다.

> ▶ **현재 값** (`packages/core/src/config.ts`의 `DEFAULT_CONFIG`가 유일한 진실. 아래는 요약이며 `sim` = Phase 0 계측으로 결정, `play` = 플레이 후 조정 필요):

```ts
// 월드
DENSITY_MIN / DENSITY_MAX       = 0.10 / 0.35     // sim: 8%는 빈 땅, 35%는 강제 추측 6%·피격 14/1000
BIOME_NOISE_SCALE               = 48              // play
distanceRamp / Cap              = 0.00025 / 0.12  // play: 100칸당 +2.5%p
terrain*                        = minRadius 8, mountain scale 16 / >0.68, river scale 96 / width 0.009, ford scale 10 / <0.32   // play
startSafeRadius                 = 3
play.cascadeRadius / Cap        = 24 / 800        // 저밀도 무한 캐스케이드 방지
fog (시야 반경)                  = 주 거점 16, 거점 9, 연 칸 주변 2(정사각형), 성장 없음   // 사용자 결정
tiers (채굴 티어)                = 주 거점에서 24/48/88/128칸부터 2/3/4/5티어, 지뢰 가치 ×1/×1.5/×2/×3/×4   // play
blast.radiusByTier              = 3–5 / 3.5–5.5 / 4–6 / 4.5–6.5 / 5–7                                     // 사용자 결정(티어별), 값은 play

// 솔버
T3_MAX_CELLS                    = 32              // sim: 반경 8 창의 컴포넌트 p95 = 26~34
T4_MAX_CELLS                    = 48              // sim: p99 = 35~47, 솔버 p99 < 4 ms
solutionCap / nodeBudget        = 200k / 4M

// 지연 결정
INTERVENTION_MODE               = FAIR (설정에서 STRICT/FORGIVING 선택, 새 게임부터)
ESCAPE_CAP                      = 64              // sim에서 검증 불가(봇 개입 0회) → 유지
forgivingRescues                = 3
debtPressureMax                 = 0.5

// 경제
BASE_VALUE                      = 10              // play
densityMultiplier(d)            = 2^((d − 0.10) / 0.06)   // sim: 12%→35% 사이 정지율 5배·피격 0→14
settlementPayoutMult            = 1, incomePerTurnMult = 0.02
streakMultPerClick / Cap        = 0.02 / 3
업그레이드 가격                  = T1a 150 · T1b 600 · T2a 2.5k · T2b 8k · T3 20k · T4 60k
                                  드론 수 2.5k×2.2^n · 속도 800×1.8^n · 반경 1.5k×2^n · 안전모드 6k
                                  감사 400 · 감사+ 1.6k · 스캐너 300/900 · 프로브 2.5k   // play: 전부 초안
prestigeDivisor / BonusPerCore  = 40 / 0.10, prestigeMinOwned = 50

// 피격 (체력 없음, §7.1 구현 블록)
MINE_HIT_LOSS_FRACTION          = 0.5             // play
WRONG_FLAG_LOSS_FRACTION        = 0.25            // 오답 깃발 1개당 미정산 자금 소각 비율
```

---

## 16. 하지 말 것 (안티패턴)

1. **깃발에 즉시 피드백 주기** — 게임 붕괴 (INV-2)
2. **드론에 확률 추측 주기** — 플레이어가 할 일이 사라짐 (INV-3)
3. **수입 감쇠 / 시설 유지보수 메커닉** — 밀도 지수 축이 이미 구식화를 처리함
4. **무추측(no-guess) 보드 보장** — 무한 보드에선 불필요하고, 보드 분포에 편향을 만들어 체감 난이도를 떨어뜨림. "강제 추측 없음"만 보장 (§5.2)
5. **모든 미확정을 50%로 표시** — 정보 왜곡 (INV-5)
6. **UI부터 만들기** — Phase 0 계측 없이는 밸런싱 근거가 없음
7. **칸을 DOM 노드로 렌더링** — 무한 격자에서 즉사
8. **전역 지뢰 수 제약을 도입** — 무한 보드와 모순. 카운팅 추론은 스캐너 아이템으로 (§11)

---

## 17. 구현 현황 (2026-09-21)

### 17.1 상태

| Phase | 상태 | 비고 |
|---|---|---|
| 0 헤드리스 코어 + 계측 | 완료 | `npm run sim`, 결과 `packages/sim/results/phase0.md` |
| 1 플레이 가능한 무한 지뢰찾기 | 구현 완료 | 20분 수동 플레이 검증 **미실시** |
| 2 경제 | 완료 | INV-2 테스트 통과 |
| 3 자동화·오염 | 완료 | 오염 역추적 테스트 통과, 바이옴·지형 벽(산·강)·스캐너 포함 |
| 4 트리아지·프레스티지 | 완료 | 청산(A안) |
| 테스트 (§14) | 57개 통과 | T-INV1~5, T-SOLVE, T-SEAL, T-SETTLE(정합성 일반화), T-TERRAIN, T-FOG, T-TIER, 시작 기준 맵, 저장 왕복, 스캐너, 거점·폭발·복구·업그레이드 |
| Electron | 개발 실행 | 패키징은 의도적으로 미설정 |

### 17.2 스펙 대비 변경 요약

1. **정산 단위**: CSP 컴포넌트 폐쇄 → 봉인 숫자에 둘러싸인 깃발 묶음(claim batch). §9.2 구현 블록.
2. **오답 깃발 페널티** 추가: batch 몰수 + 미정산 자금 소각. 스펙에 없던 규칙이며, 없으면 깃발 스팸이 무료 프로브가 된다.
3. **6티어 솔버**: T1/T2 분할. §4.3 구현 블록.
4. **커밋 정책**: 안전 공개 칸은 커밋하지 않고 진실의 무상태 안정성에 의존. 개입은 컴포넌트 전체 대안 해 커밋. §5.1 구현 블록.
5. **밀도 부채**: 청크 최초 관측 시 압력 동결. §5.3 구현 블록.
6. **피격**: 체력 대신 미정산 자금 손실(Sapper 방식). §7.1 구현 블록.
7. **캐스케이드 상한**: 저밀도 무한 확산 방지.
8. **프로브**: 지뢰면 즉시 소유(별도 "확인된 지뢰" 상태 없음).
9. **드론 작업 범위 = 원형**, 드론은 소유 지뢰 위에 서고, 행동은 대상 선정 → 선 뻗기 → 진행도 충전 → 실행. 지뢰 사이 이동은 배치 지점 반경으로 제한. 다음 대상은 직전 행동 칸(선 끝, `tip`)에서 가장 가까운 칸을 고르고 선도 거기서 이어 뻗는다(드론으로 되돌아가지 않음). 정지·이동·드래그 시에는 드론 위치에서 다시 시작. §8.2 구현 블록.
10. **완료된 숫자 제거**: 주변에 Unknown이 없는 숫자는 미정산 깃발이 남아 있는 동안 75 % 투명도로 흐려지고, 그 깃발까지 정산되면 서서히 사라진다. 공개 타일은 카드처럼 뒤집히고(덮개가 접힌 뒤 숫자가 펼쳐짐), 클릭한 칸(캐스케이드 시작 칸 `CellChange.from`, 코드는 누른 숫자)에서 퍼지듯 열린다.
11. **시작 = 전부 안개**: 새 월드는 전체가 안개에 덮인 채 시작한다. 첫 클릭은 칸 열기가 아닌 **주 거점 설치** 판정이라 안개 위에도 놓이고, 그 칸이 주 거점·무지뢰 시작 영역(`startSafeRadius`)·거리 램프의 중심이 되며 시작 영역이 바로 열린다(`World.setStart`, 진실이 한 번이라도 관측되면 고정, 저장됨). 맵(바이옴·지뢰·지형)은 시작 칸 기준 상대 좌표로 생성되므로(`world.startRelative`) 어디를 누르든 주 거점 주변 맵은 같다: 설치 위치 선택은 사실상 "시작" 버튼이다. 이전 저장은 절대 좌표 맵을 유지한다.
12. **거점 경제**: 소유 지뢰는 거점, 시작 칸은 주 거점(직접 생산 없음). 주 거점 레벨이 생산량을 결정한다. 거점은 주 거점을 향한 트리(범위 제한 없음)로 연결되고, 연결된 거점은 여는 행동(턴)마다 생산량만큼 크레딧을 올린다(연결선 화물은 연출). §9.3 구현 블록.
13. **폭발·복구**: 피격 시 원형 폭발이 범위 안 거점을 비활성화(주 거점 면역), 열린 칸 수에 비례하는 비용으로 복구. 반경은 지뢰가 있는 채굴 티어의 범위 안에서 무작위(`blast.radiusByTier`, 거리 5칸 구간 규칙은 폐기). §9.3 구현 블록.
14. **업그레이드 축소**: 주 거점 레벨·연속 보너스 상한·채굴 기술만 판매(운송 속도는 턴 생산과 함께 제거). 솔버 티어 자동화·드론·소모품·확률 오버레이 제거(드론 로직은 장착 아이템용으로 유지).
15. **지형 벽**: 산·강은 타일·지뢰·숫자가 없는 벽. 벽에 붙은 모호 컴포넌트는 강제 추측이 되고 FAIR도 구제하지 않는다(§0.4-3 뒤집힘). 지뢰 띠 강은 제거. §3.3, §5.2 구현 블록.
16. **전장의 안개**: 시야원이 본 적 없는 닫힌 칸(Unknown·깃발·벽)은 점선 테두리의 희미한 타일로 가려져 산·강을 구분할 수 없고, 열거나 깃발을 꽂을 수 없다. 시야원: 주 거점(원 반경 16), 모든 거점(원 반경 9, 생성 즉시), **연 칸마다 주변 2칸**(정사각형). 반경은 고정(성장 개념 제거). 연 칸이 곧바로 주변을 밝히므로 캐스케이드는 안개에 막히지 않는다. 한 번 밝혀진 곳은 다시 가려지지 않는다. `packages/core/src/fog.ts`.
17. **채굴 티어(테크트리)**: 주 거점에서 거리(유클리드) 24/48/88/128칸부터 2/3/4/5티어(`config.tiers`; 2026-09-22 기존 48/128 구간을 반씩 쪼갬). 1티어는 그냥 캘 수 있고, n티어는 주 거점 패널의 채굴 기술 `mining`(하나의 레벨형 기술, 2026-09-22 통합)을 n−1레벨까지 올려야 열기·깃발·코드가 가능하며 캐스케이드도 경계에서 멈춘다. 상위 티어 지뢰는 가치(정산금·생산) ×1.5/×2/×3/×4, 폭발 반경이 더 크다. 레벨 가격은 10k/65k/400k/1.05M으로, 직전 티어 전체 지뢰 정산 가치(streak ×1 기준 17k/108k/663k/1.77M)의 약 60% — 한 티어를 대부분 캐면 다음 티어를 살 수 있다(2026-09-22). 티어별 색 구분은 없고, 잠긴 타일은 전부 고유 색 + 자물쇠로 표시되며 기술을 배우면 일반 Unknown 타일로 바뀐다.
18. **정산은 여는 행동에서만, 코드는 구제 없음**: 깃발 행동은 정산을 일으키지 않고 다음 여는 행동이 정산한다. 코드로 연 칸은 개입 없이 진실대로(오답 깃발이면 폭발). §9.2 구현 블록.
19. **턴 생산**: 여는 행동 1회 = 1턴, 턴마다 연결 거점 생산분 즉시 입금. 숫자를 누르면 주변의 조작 가능한 Unknown 타일이 눌리는 연출. §9.3 구현 블록.
20. **타이틀 화면**: 실행할 때마다 MINEWORLD 타이틀(상단 중앙)과 큰 시작 버튼(하단 중앙; 세이브가 있으면 이어하기 + 작은 새 게임 버튼, 새 게임은 확인 후 코어 유지). 배경은 저장되지 않는 임시 월드를 간단한 봇(전 티어 솔버, 막히면 가장 안전한 칸 추측)이 플레이하는 데모이며, 필름 그레인·스캔라인·비네트·깜빡임으로 녹화 영상처럼 보이게 한다(데모 월드는 채굴 티어 끔). 시작을 누르면 타이틀은 위아래로 화면 밖으로 사라지며 투명해지고, 데모가 사라진 뒤 플레이어의 월드 타일이 화면 좌상단에서 우하단으로 대각선 물결처럼 생겨난다. 새 월드(주 거점 없음)면 "아무 곳이나 클릭해 주 거점을 세우세요" 안내가 첫 클릭까지 표시된다.

### 17.3 남은 일

- 스캐너 판매(벽에 막힌 50/50의 리스크를 크레딧으로 사는 수단, 방식·가격 미정).
- 12% 월드 20분 수동 플레이(Phase 1 수용 기준)와 그에 따른 `play` 파라미터 조정(가격, BASE_VALUE, 피격 손실률, 바이옴 스케일).
- T3의 가치가 계측상 미미하다. 유지할지, T2b를 약화해 T3에 역할을 줄지 플레이 후 결정.
- Electron 패키징(electron-builder)은 필요 시 추가.

### 17.4 사용자 결정 기록

범위 Phase 0~4 전체 · 스택 Vite+TS+PixiJS · 피격 = 자원 손실 · UI ko/en, 코드는 영어 · 입력 클래식/토글 둘 다 · 프레스티지 A · 테마 라이트/다크 자동(VS Code 스타일) · Electron 개발 실행만 · 드론 = 소유 지뢰 거점 + 선 연출 + 드래그 재배치, 이동은 배치 지점 반경 제한(2026-09-21) · 거점: 주 거점 루트 트리(다른 거점 경유, 범위·연결 보너스 없음), ㄱ자 타일 연결선, 생산물 운송 후 주 거점 도착 시 입금, 업그레이드는 주 거점만, 주 거점은 첫 클릭부터 생산, 1칸 이내 거점은 단지(한 노드), 상하좌우에 열린 칸 없는 단지는 고립(생산·연결 없음)(2026-09-21) · 단지는 상하좌우 연쇄만, 테두리 표시 · 주 거점 직접 생산 제거, 레벨당 ×1.15 · 폭발 반경 = 주 거점 거리 5칸 구간별 3–5/4–5/4–6/5–6… 무작위, 주 거점 면역, 비활성 거점 복구 비용은 열린 칸 수 비례 · 업그레이드는 주 거점 레벨 + 운송 속도 + 연속 보너스 상한, 자동화·드론·소모품 제거(드론 로직 유지) · 도착 화물 꼬리는 잔상으로 페이드(2026-09-21) · 단지 = 정착 거점(주변 숫자 소멸) 2칸 이내 연쇄, 연결선은 Unknown·깃발 우회, 경로 없으면 고립, 거점 5개 이상 대단지는 ×(1 + 0.05 × 거점 수) + 형성 연출, 단지 테두리 투명도 절반(2026-09-21) · 지형 벽(산·강, 칸·숫자 없음)으로 강제 50/50을 의도적으로 만듦, 벽 접촉 시 FAIR 구제 없음, 지뢰 띠 강은 벽 강으로 교체, 벽은 거점 정착에선 해결된 칸·연결선은 차단, 스캐너는 나중에(2026-09-21) · 전장의 안개: 시야원은 주 거점·거점만(칸을 여는 것은 시야를 주지 않음), 반경이 시간에 따라 최대치까지 넓어지고 다시 가려지지 않음, 비활성·고립 거점은 성장 정지, 주 6→16·거점 2→6·30초당 1칸(2026-09-21) · 주 거점 시야는 처음부터 최대(성장 없음), 거점 시야 1.5배(3→9), 성장은 시간 대신 해당 거점 자원의 주 거점 도착 5회당 1칸 · 우클릭 순환 깃발 → ? → 해제, ?는 판정 없는 단순 표식(2026-09-21) · 안개 타일 = 점선 테두리 + 희미한 타일, 전체 안개에서 시작하고 첫 클릭은 안개 위 주 거점 설치(+ 시작 구역 열기), 맵은 주 거점 기준 상대 좌표, 시야 성장 제거(주 16·거점 9 고정), 연 칸 주변 2칸 시야 · 채굴 티어 테크트리(주 거점 거리 링, 잠긴 티어는 조작 불가, 상위 티어 지뢰 가치 배수), 폭발 반경은 거리 구간 대신 티어별, 채굴 기술은 주 거점 패널 섹션(2026-09-21) · 깃발 행동은 정산하지 않고 다음 여는 행동 때 정산, 코드는 구제 없이 오답 깃발이면 폭발, 생산은 여는 행동 1회 = 1턴마다 즉시 입금(화물은 연출만, 운송 속도 제거, 턴당 값 = 이전 초당 값), 숫자 클릭 시 주변 Unknown 눌림 연출(2026-09-21) · 타이틀 화면: 배경 = 실제 엔진 + 봇 데모(영상 아님), 매 실행 표시 + 이어하기/새 게임 분리, 필름 연출은 그레인·스캔라인·비네트만(REC 라벨·타임코드 없음), 안개 타일은 대각선 물결로 생성, 타일 공개는 클릭 지점에서 퍼지는 뒤집기(2026-09-22).
