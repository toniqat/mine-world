{
  "name": "@mine/electron",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build:main": "tsc -p tsconfig.json && node -e \"require('fs').copyFileSync('src/preload.cjs','dist/preload.cjs')\"",
    "dev": "npm run build:main && concurrently -k -s first \"vite --config ../web/vite.config.ts ../web\" \"wait-on http://localhost:5173 && cross-env ELECTRON_RUN_AS_NODE= VITE_DEV_SERVER_URL=http://localhost:5173 electron .\"",
    "start": "npm run build:main && vite build --config ../web/vite.config.ts ../web && cross-env ELECTRON_RUN_AS_NODE= electron ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^33.2.0",
    "wait-on": "^8.0.1"
  }
}