# @mine/web

Browser client. Vite + TypeScript + PixiJS (WebGL) for the board, plain DOM for the chrome. No framework.

## Files

| File | Responsibility |
|---|---|
| `index.html` | Shell: `#stage` (Pixi canvas), `#hud`, `#panel`, `#toasts`, `#modal`. |
| `src/main.ts` | Boot: load save from IndexedDB, create `App`. |
| `src/demo.ts` | `DemoPlayer`: the title-screen demo. A throwaway `Game` (never saved, no tiers, no pool cap) played by a small bot, one action every ~0.44 s: it digs one patch outwards from the start (never farther than `REACH` = 30 tiles), solving the patch with every tier and taking verdicts nearest the start and its last cell; when nothing is determined it guesses the least likely mine in reach. Three guesses in a row, nothing left in reach or 500 actions set `over`; `next()` makes a fresh world. `view` is a slowly smoothed focus for the camera. |
| `src/app.ts` | Orchestrator: owns `Game`, camera, renderer, input, HUD, panels, the title screen (plays the demo at 94 % zoom with the camera drifting after `DemoPlayer.view`, HUD and input off; when the demo is `over` the board fades out while the title's black layer fades in, the next world is made in the dark, then it fades back in; Start / Continue / New game fade the demo out, swap in the player's world and run the tile intro, then bring the HUD and input back; the start hint until the main base is placed), chords (`chordAt`: ripple from the chorded number via `BoardView.rippleFrom`; the number's actionable Unknown neighbours that stay closed get the press effect), base selection (clicking a base opens the Base popover; clicking the main base or `U` opens the main-base popover), a full pool (an opening or chord refused with `full` shakes the pool capsule and toasts that Cash Out comes first), Cash Out (the HUD counters show the pool draining into credits), repairs, hit feedback (toasts + tile blast), grand complex formation (effect + toast), fog lifting (`fog` event -> `liftFog`), mining technology learned (`tech` event -> `refreshAll` + toast), a toast naming the mining level a locked cell needs, drone drag and drop onto Owned mines (only matters once drones are equipped), autosave, prestige and new-game flows. A new world is all fog: the view centres on the start cell, the start hint says to click anywhere, and the first click places the main base (and opens the start area). |
| `src/render/textures.ts` | Vector-drawn cell atlas per theme (tiles, "?" mark tile (and its hover), flag + bare flag glyph, owned base dot (plus translucent isolated and struck-through disabled) / lost / exploded marks, single-colour digits, terrain walls: full-bleed mountain / water fills with a faint glyph so neighbouring walls merge, fog of war: a faint tile with a dashed outline that hides whether a wall lies under it; closed tiles per mining tier (`tiers`, `tierTextures`: Unknown, hover, flag, locked with a padlock)). `CELL = 32`. |
| `src/render/camera.ts` | World/screen transform, zoom-at-cursor, visible cell range. |
| `src/render/boardView.ts` | Chunked sprite renderer (one centred sprite per cell, culled per 16×16 chunk, all chunks repainted once the first click sets the start so terrain appears, digits hidden below 50 % zoom, fogged cells painted with the fog tile, `liftFog()` repaints cells the fog lifted from under a fog cover fading out over 600 ms with a little jitter, Unknown / flagged cells painted with the plain tile, or the locked tile (`cellLocked` + padlock) while their tier is locked, `refreshAll()` after a technology, no hover on fogged (except before the start) or locked cells, an opened cell's cover is the tile it showed, "?"-marked Unknown cells painted with the "?" tile (hover refreshed after every change), numbers dim to 25 % opacity once only unsettled flags are left next to them and fade out once neither Unknown nor unsettled flag is) + overlays: complexes of two or more bases (member tiles plus the rectangles between members within reach tinted faintly with the accent, and a faint round-cornered border traced around the group), the base network (edge paths as tile strips a sixth of a tile wide at 10 % opacity, built from non-overlapping pieces), the selected base's route to the main base and its complex, a large diamond on the main base, probabilities, drone work discs (tint on Unknown cells only), drop targets while dragging, scanner boxes, targeting preview, highlights, density heatmap. Per-frame effects: press (`press()`: clicking a number shrinks its closed neighbours' tiles about their centres and springs them back), shipments (cosmetic, a stretch at most 45 % opaque with a smooth fade-in front and fading tail, moving along the network towards the main base, hidden inside complexes of two or more bases so goods seem to leave and enter through the complex border; when one arrives or hands over to the next edge its trail stays as an afterimage and fades out in place), blasts (every tile inside the blast radius flashes hot, pops and cools to red in a wave from the centre with a little jitter, then a faint scorch fades), grand complex formation (`grand()`: member pulse, two rings spreading from the complex's centre, a label rising and fading), flag pop-in/out, reveal flip (the tile it showed folds to an edge while darkening, then the number unfolds; ripples out from the clicked cell), tile intro (`intro()`: tiles pop in in a diagonal wave from the top-left of the screen, overlays fade in after), drone lines (grow to the target, ride to another mine, abandoned lines shrink back; a line the next one continues from shrinks towards its end), clock-wipe on the target, settlement pulse ripple. |
| `src/input.ts` | Pointer/touch/keyboard (`enabled` off during the title and intro): click vs drag, object grab (`grab` / `grabMove` / `grabEnd`, used for drones), long-press flag, wheel/pinch zoom, inertia, WASD/arrow pan. |
| `src/ui/hud.ts` | Three floating capsules, no bar: credits (top left), the unbanked pool with its cap and fill bar, the combo with the point multiplier, and Cash Out (top centre; warning colour and a pulsing button when full, `nudgeFull()` shakes it), and the menu (top right: flag mode in toggle input, home, settings). `Counter`: the shown credits and pool count unit by unit towards the real values, faster the bigger the gap (`MIN_RATE`, `CATCH_UP`). Pool gains and losses float up as "+N" / "−N" (collected over `FLOAT_MS`); `cashedOut()` marks a Cash Out's drop as banking, not loss. |
| `src/ui/panels.ts` | Popover under the top-right capsule (rounded, scrolls vertically when long; drops in from a little above while fading in, lifts away on close): Settings and the Base panel. Main base: title with its level, level-up cost and button in the header; how points work, the point multiplier from bases, active / isolated / disabled base counts, the pool cap and the next level's cap, the upgrades (streak cap) and a Mining technology section (one levelled `mining` row: the highest tier minable now, and where the next tier begins and its blast range). Other bases: isolation notice, its share of the multiplier, complex size, grand complex bonus, route to the main base in tiles / hops, bases routed through, next hop. Disabled bases: notice, repair cost and Repair button. |
| `src/ui/title.ts` | `TitleScreen` (MINEWORLD logo, Start or Continue + New game, a black layer under the film for the demo's world change (`setBlack`), film layer: generated grain tile, scanlines, flicker, vignette; `leave()` slides it away) and `StartHint`. |
| `src/ui/toast.ts` | Toasts and the confirm dialog (backdrop fades in, the box drops from a little above; closing plays it backwards). |
| `src/ui/dom.ts` | `el()` helper, icons (settings, home, flag). |
| `src/theme.ts` | VS Code Light Modern / Dark Modern palettes for CSS and Pixi (plus `cellMountain` / `cellWater`, the fog tile outline `cellFog` and the locked-tier tile colour `cellLocked`). |
| `src/style.css` | Chrome styling (flat, 1 px borders, single accent), HUD capsules (gain floats, full-pool pulse and shake), popover and modal drop-in transitions, title screen, demo black layer and film layer, HUD slide-out while `#app` is `.titling` or `.intro`. |
| `src/i18n.ts` | `t()` with Korean and English tables. |
| `src/format.ts` | Number / time formatting. |
| `src/storage.ts` | IndexedDB save (`SaveData` structured clone), localStorage settings. |

## Controls

- Title: Start (or Enter) begins; with a save, Continue resumes and New game replaces the world (cores are kept).
- Drag / wheel / pinch: pan and zoom. WASD or arrows also pan. `H` returns to the main base.
- Click a base (Owned mine) to open its panel; a disabled base (grey dot, struck through) is repaired there. The main base (start cell, large diamond) and `U` open the main-base panel, where it is levelled up (raising the pool cap) and the upgrades are bought.
- Points: every opened tile earns 1 and every new base 5, times the multiplier (1 + 0.1 per linked base, times the combo), into the pool (top centre). Once the pool reaches its cap nothing more opens until Cash Out moves it into credits (top left).
- A new world is all fog; the first click places the main base there. Fogged tiles (dashed outline) cannot be opened or flagged; the main base sees 16 tiles, a base 9, and every opened cell 2 around it.
- Coloured tiles with a padlock are in a mining tier the mining technology (main-base panel, *Mining technology*) does not reach yet; levelling it up turns them into plain tiles.
- Classic mode: left-click opens, right-click or long-press cycles flag -> "?" -> clear (a "?" is only a note), clicking a number chords. Flags settle (become bases) on the next opening, never by themselves.
- Toggle mode (Settings): the flag button in the top-right capsule or `F` switches open/flag for taps.
- `Space` cashes out. `U` opens the main base. `Esc` closes.

## Scripts

```
npm run dev        # http://localhost:5173
npm run build      # dist/
npm run typecheck
```
